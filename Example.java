class Example {
    public static void main(String[] args) {
        int result;
        boolean boolresult;
        int[] array;

        A csa;
        B csb;
        csa  = csb;
        result = csa.foo(2, 2);
        System.out.println(new A().foo(10,20));
    }
}

class A{
    int i;
    A a;
    boolean geia;

    public int foo(int i,int j) { 
        int keepo;
        int jim;
        int result;
        boolean boolresult;
        int[] array;
        boolean kipo;
        

        array[this.foo(1,2)] = a.foo(result,keepo);
        array[jim] = this.foo(jim, keepo);
        
        if(this.bar(true)) {
            result = this.foo(21, 1);
            boolresult = kipo && true;
        }
        else{
            boolresult = kipo && true;
        }

        result = this.foo(21, 1);
        array[jim] = result;
        result = array.length;
        result = array[jim];
        result = keepo + jim;
        result = keepo * jim;
        boolresult = kipo && true;
        array = new int[jim];

        return i+j; 
    }
    public boolean bar(boolean kap){ int pasios; return 1; }
}

class B extends A {
    int i;
    boolean eggs;
    
    
    

    public int foo(int i, int j) { 
        int malak;
        int egw;
        int res;
        boolean resbool;
        A classres;
        boolean eggs;
        
        while(a.bar(this.bar(geia))) System.out.println(this.bar(eggs));
        System.out.println(this.bar(eggs));
        res = egw + malak;
        res = egw + 321;
        res = egw - malak;
        resbool = egw < malak;
        resbool = geia && !eggs;
        classres = new A();
        resbool = a.bar(eggs);
        return i+j; 
    }
    public int foobar(boolean k){ return 1; }
}
